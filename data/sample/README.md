# Données d’exemple

- **`books_sample.csv`** : interactions **lecteur → livre** (100 lignes, 10 colonnes) — démo recommandation de livres. Colonnes principales pour le mapping : `reader_id`, `book_id`, `rating`, puis métadonnées (`rated_at`, `title`, `author`, etc.).
- Régénération : `python scripts/generate_books_sample.py`.
