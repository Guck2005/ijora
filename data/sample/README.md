# Données d’exemple

- **`u_sample.dat`** : quelques interactions au format proche **MovieLens 100k** (`user_id`, `item_id`, `rating`, `timestamp`, séparateur tabulation).
- Le jeu complet **MovieLens 100k** peut être téléchargé depuis le site [GroupLens](https://grouplens.org/datasets/movielens/100k/) pour les tests de performance des sprints suivants (ne pas commiter le fichier complet si non nécessaire).
