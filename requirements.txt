# --- Ijora / RecoSaaS — dépendances TP Big Data & ML ---
# Versions figées pour reproductibilité des TPs.
#
# INTERDIT pour le cœur recommandation (filtrage collaboratif from scratch) :
#   scikit-learn, surprise, implicit, ou tout package équivalent.
# L’algorithme item-item utilise uniquement pandas + numpy (code maison).

streamlit>=1.35,<2
pandas>=2.0,<3
numpy>=1.26,<3
plotly>=5.18,<6
fpdf2>=2.7,<3
pytest>=8.0,<9
ruff>=0.4,<1
